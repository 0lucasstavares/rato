/target

# Session handoff (local only, not versioned)
handoff.md

# API keys — never version (keep the folder via .gitkeep)
/keys/*
!/keys/.gitkeep

# tauri shell build artifacts (excluded from root workspace, has its own target)
/apps/shell/src-tauri/target/
